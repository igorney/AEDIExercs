#include <stdio.h>

int main()
{
    long n, num, v[100000];
    long i, aux;
    scanf("%ld", &n);
    for (i = 0; i < n; i++)
    {
        scanf("%ld", &v[i]);
    }
    scanf("%ld", &num);
    for (i = 0; i < n; i++)
    {
        
    }
    printf();

return 0;
}