#include <stdio.h>

int main(){
	float x, y, z;
	int t=0, e=0, p=0, s=0;
	while(x != 0 && y != 0 && z != 0){
		scanf("%f %f %f", &x, &y, &z);
		
		printf("Caso #%d: %.0f Especial, %.0f Predileta e %.0f Sortida\n", t, e, p, s);
	}
	return 0;
}
